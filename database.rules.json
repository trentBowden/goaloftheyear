{
  "rules": {
    "votes": {
      "women": {
        ".read": true,
        "$userId": {
          ".write": true,
          ".validate": "newData.hasChildren(['userId', 'goalId', 'category', 'timestamp']) && newData.child('category').val() === 'women'"
        }
      },
      "men": {
        ".read": true,
        "$userId": {
          ".write": true,
          ".validate": "newData.hasChildren(['userId', 'goalId', 'category', 'timestamp']) && newData.child('category').val() === 'men'"
        }
      }
    }
  }
}
